# 评审功能落地路线

本路线以 `deepseek-harness-launcher-review.zip` 中针对 v1.0.10 的 28 项建议为输入，
并以当前仓库代码和 `CURRENT_DESIGN.md` 为最终依据。评审中的实现步骤只作为建议，
不会覆盖后来已经确定的产品行为。

状态：`完成`、`部分完成`、`待实现`、`调整后实现`。

| 编号 | 内容 | 当前状态 | 落地说明 |
| --- | --- | --- | --- |
| F-03 | Plugin 更新角标与批量更新 | 完成 | 市场已有版本角标；批量更新逐项校验、快照、回档并汇总。 |
| B-04 | zstd 会话错误隔离 | 完成 | 文件级异常隔离、头部读取上限和损坏条目标记已有测试。 |
| F-16 | 置顶、最近内容与快速恢复 | 部分完成 | 已记录最近使用实例；保留当前“显示全部实例”的要求，后续只补置顶与最近会话。 |
| B-02 | GitHub API 配额处理 | 完成 | Plugin 与 Skill 市场共用条件请求缓存；支持 ETag / Last-Modified、剩余配额、限流恢复时间，以及由当前 Windows 用户 DPAPI 保护的可选 Token。 |
| B-03 | DPAPI 跨账户边界 | 完成 | 保留 `.dshsnapshot` 本机 DPAPI 回滚点；新增 PBKDF2-SHA256 + AES-GCM 的 `.dshpsnapshot` 跨电脑密码导出与导入。 |
| B-05 | loopback 端口长期冲突 | 部分完成 | 已有持锁换端口重试；成功端口复用与失败端口避让待补。 |
| R-03 | 数据安全关键路径测试 | 部分完成 | 保留 SelfTest，并新增正式 xUnit 套件覆盖配置迁移、密码快照、整合包脱敏、会话同步、GitHub 条件请求、主题能力探测及旧凭据导入转换；后续随相关修改继续迁移。 |
| R-04 | 管理导航收敛 | 部分完成 | 扩展、Agent、Provider、对话、日志和空间管理已嵌入主导航；Chat 继续保持独立任务栏窗口。 |
| F-02 | 对话全文搜索 | 完成 | 搜索 JSONL/Zstandard 的标题、工作区和正文；单文件限制读取 32 MiB，损坏文件独立跳过，不改写上游 SQLite。 |
| F-04 | 实例资源监控 | 完成 | 每 3 秒采样 Launcher 管理的进程树，显示 CPU、内存与运行时长；停止后清除瞬时数据。 |
| F-06 | Plugin 兼容性预检 | 完成 | 安装前读取 `engines` / `peerDependencies` 的 DSh 版本范围；不兼容项明确警告，批量更新直接跳过。 |
| U-01 | 列表键盘操作 | 待实现 | 为实例、对话和已安装列表补选择、打开、删除与查找快捷键。 |
| B-01 | 会话直达能力探测 | 待实现 | localStorage 失效时明确降级为只打开 Chat。 |
| F-05 | 空闲停止与崩溃守护 | 完成 | 每版本可独立关闭；仅作用于 Managed 实例，按 5 秒至 5 分钟退避并限制 5 次，稳定 10 分钟后重置计数。 |
| F-07 | 全局诊断包与日志页 | 完成 | 设置页可查看 7 天 Launcher 日志并导出脱敏诊断 ZIP，继续复用已有 crash.log 和 Plugin 原始失败报告。 |
| F-08 | 存储管理 | 完成 | 按实例统计分类占用，先逐文件预览；确认后只把自动快照、报告和明确缓存移入回收站。 |
| F-09 | CLI 与 URL 协议 | 完成 | CLI 和 `dsh-launcher://` 复用单实例通道，支持打开、启停、重启、Chat、版本设置、Plugin 与对话页，并可创建实例桌面快捷方式。 |
| F-10 | 托盘与状态通知 | 待实现 | 与空闲停止、崩溃守护同时落地。 |
| F-11 | 整机搬家包 | 待实现 | 与现有 dshpack、ModPack 和快照能力复用；凭据必须采用跨机密码加密。 |
| F-12 | 本地 Plugin 开发模式 | 待实现 | 停止实例使用链接，运行实例只走已验证的 dsh-market 热加载。 |
| F-13 | Attached 实例自动发现 | 待实现 | 保留只读语义，不自动接管或终止外部进程。 |
| F-14 | 本地用量统计 | 待实现 | 只统计 Launcher 能安全读取的会话格式。 |
| F-15 | 每版本环境变量与启动参数 | 待实现 | 复用版本设置和启动命令工厂，敏感值不明文落盘。 |
| F-01 | 统一任务中心 | 完成 | 顶栏集中显示下载、运行环境、Plugin 与导入任务，支持真实进度、取消、失败重试和最近 50 条持久历史。 |
| R-01 | 拆分 ExtensionWindow | 部分完成 | 已把 dsh-market 主题状态、能力探测和 Chat 同步抽到独立控制器；后续仍只在触及相关区域时渐进拆分。 |
| R-02 | 可复现发布流水线 | 完成 | Windows CI 固定 .NET SDK、锁定 NuGet 依赖，运行 build、xUnit 与 SelfTest；`v*` 标签校验项目版本后生成固定名 EXE、版本元数据和 SHA-256，并安全创建草稿 Release。 |
| R-05 | 配置 schema 迁移 | 完成 | Launcher 的实例注册、全局设置和版本设置统一写入 schema v1；旧格式原子迁移并保留备份，较新未知 schema 拒绝改写；不处理 DSh 官方配置。 |
| U-02 | Chat 主题联动 | 完成 | 通过 DSh 上游 `settings.describe` 探测 `ui-theme.preference`；仅确认支持且 Chat 已打开时启用联动，不注入 Launcher 私有接口。 |

## 实现顺序

1. Plugin 维护闭环：F-03、F-06。
2. 对话可见性：F-02、B-01、U-01。
3. 运行状态：F-04、B-05、F-13。
4. 长任务地基：F-01，再承接下载、安装、同步和诊断任务。
5. 后台自动化：F-05、F-10、F-07、F-08。
6. 外部入口与网络韧性：F-09、B-02、R-02。
7. 迁移和高级能力：F-11、F-12、F-14、F-15、B-03、U-02、R-03。
8. 渐进结构收敛：R-01、R-04、R-05；每次只迁移本轮实际触及的区域。

每一批都保持可独立测试、可回退，不为后续批次提前引入未使用的框架。
