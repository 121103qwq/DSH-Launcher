# DSH Launcher Bug 记录

## 已解决（本轮代码回归）

以下四项已由 33 项 xUnit 回归、76 项 SelfTest 及调用链检查验证；本轮未做 Computer Use 验收。

### 版本快照漏存 Plugin 构建许可

- 现象：安装或更新 Plugin 后，恢复版本快照仍保留修改后的构建许可。
- 原因：完整快照的 Profile 文件清单遗漏 `pnpm-workspace.yaml`。
- 修复：纳入本机及密码快照；兼容旧快照，不删除旧快照未管理的该文件。

### GitHub 主题预览大小限制检查过晚

- 现象：超过 8 MiB 的 GitHub 图片仍完整下载到内存，之后才被预览拒绝。
- 原因：接入共用 GET 缓存后，调用方检查大小时响应体已经读取完毕。
- 修复：请求携带大小上限，已知长度提前拒绝，未知长度限量读取；缓存不能绕过更严格的上限，超限后继续尝试下一张图。

### GitHub 配额检查漏处理网络超时

- 现象：网络超时后仍显示正在读取，异常进入全局处理。
- 原因：仅捕获用户取消，漏掉内部请求超时的 `OperationCanceledException`。
- 修复：独立显示超时提示，保留取消提示，并在结束后恢复刷新按钮。

### 切换 Profile 后沿用旧主题能力

- 现象：在 web Profile 探测成功后切到其他 Profile，Chat 主题同步仍可能可用。
- 原因：Profile 切换未清除旧探测状态，异步返回也可能覆盖新的选择。
- 修复：切换时立即使旧能力失效，非 web 禁止联动，切回 web 重新探测；用选择代次忽略过期结果，状态更新留在 UI 异步调用链。

## 已解决

### 旧凭据包装导致新 DSh 启动前退出

- 现象：自动导入旧实例后，当前 `dsh-credentials-local` 报凭据值必须为字符串，健康检查前退出。
- 原因：旧 `.credentials.yaml` 使用 `version: 1` / `refs:` 包装，当前插件只接受顶层键值映射；完整目录复制绕过了兼容转换。
- 修复：导入、覆盖和 Provider 同步统一进行保守结构转换并原子写入；当前格式和无法确认的格式保持原样。
- 状态：已解决；正式单测覆盖新导入与覆盖导入，隔离 rc7 实例已实际启动并通过健康检查。

### 页面切换后当前实例被清空

- 现象：实例启动后进入扩展页再返回，列表仍高亮，但左侧显示未选择，停止和重启不可用。
- 原因：WPF 在仪表盘收起或替换列表项时会把 `SelectedItem` 瞬时写回 `null`。
- 修复：实例仍在注册列表中时忽略这类瞬时清空；删除和重新加载仍可正常改变选择。
- 状态：已解决；已实际验证启动、切换扩展、返回及停止完整链路。
