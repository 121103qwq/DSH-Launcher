# DSH Launcher 当前设计

- 技术栈：.NET 8 WPF，目标 Windows x64；发布版采用自包含单文件，不依赖 Node.js、npm、pnpm 或已经安装的 DSh。
- 界面：无边框、可调整大小的 Launcher 窗口；窗口和页面外边距保持紧凑，内容卡片不额外缩窄；Agent 页面在隐藏插件市场时跨满内容区，扩展页面才使用左侧当前实例、右侧插件市场的双栏结构。正在运行的实例列表支持双击打开或重新呼出对应 Chat 窗口；版本控制列表支持双击版本直接进入该版本的设置；版本设置主标题和左侧设置栏显示当前实例名称。标题栏中间固定显示“启动”、扩展、Agent、对话和设置导航，启动页既可从中间“启动”进入，也可点击左上角 DSH 品牌按钮返回。主窗口使用 DSH Launcher 图标；每个运行实例的 Chat WebView2 都是无 Owner 的独立任务栏窗口，使用黑色 DeepSeek 图标，关闭 Chat 不停止实例。启动页左栏默认展开 Provider/“离线配置”区域，Provider 下方才是版本选择与启动操作，右侧显示正在运行的实例；启动页不展示运行环境卡片。扩展页面左侧显示当前实例和已安装内容，右侧显示插件市场；扩展、Agent、对话管理不使用独立弹窗，统一在主窗口内容区切换；模型配置页已移除，Provider 状态和诊断保留在启动页。主窗口统一接管鼠标滚轮，在嵌套滚动容器之间选择当前仍可滚动的区域；按钮、文本框、标签页和卡片使用统一圆角风格，主窗口标题不再与内嵌页面重复。版本控制页按 PCL2 的左侧列表/右侧详情结构提供复制、干净版本、删除版本和整合包导入；选中已停止版本后“复制版本”和“删除版本”可用，删除前必须确认并清理该版本的 `DSH_HOME`、Launcher 备份和注册记录，不删除共享的 DSh 运行目录；版本设置页按 PCL2 的四项左侧导航组织为“个性化、配置、插件管理、导出”，右侧切换当前分类内容。代码、视觉和素材独立实现。
- 版本列表中的版本卡片只读取本地数据，显示 DSh 版本及当前 `DSH_HOME` 下的 Plugin/Skill 数量摘要，不触发网络请求或启动 DSh。
- 实例隔离：Manager 数据位于 `%USERPROFILE%\\Documents\\DeepSeek\\launcher`；每个实例使用 `instances\\<id>\\dsh-home`，子进程同时注入 `DSH_HOME` 和实例专用 `DSH_AGENTS_HOME`。多个版本可以共享同一个 DSh 运行目录，但必须使用不同的 `DSH_HOME`；注册记录会拒绝越界 HOME、重解析点和重复 ID；取消注册不删除实例数据。
- 生命周期：`installed` 使用注册的 `dsh` 入口，`source` 使用 Source `package.json`/`apps/cli/package.json` 声明的 Node.js engine；Runner 使用 loopback 空闲端口、HTTP 健康检查、实例独占锁和整棵子进程树清理。运行态区分 Launcher 管理的 `Managed` 与只连接已记录 loopback Web 端点的 `Attached`；Attached 可打开 Web UI，但 Stop、Restart 和退出清理都不触碰外部进程。启动成功后可打开独立 Chat WebView2 窗口；关闭 Chat 不停止实例。
- Source：读取项目根 `package.json`、包管理器字段/锁文件、构建脚本、依赖目录和 CLI 入口；准备阶段执行 install 和 `run build`，只接受实际找到的 CLI 构建入口，超时/取消/非零退出会保留诊断并清理进程树。
- 扩展：Plugin 依据 DSh `profiles\\web\\package.json` 的 dependencies 与 `dsh.profile.bundles` 管理；安装、更新、删除通过 DSh CLI 完成。调用官方 CLI 时优先使用现有 pnpm；只有当前 PATH 没有 pnpm 但有 Corepack 时，Launcher 才创建临时 pnpm shim 并仅注入该子进程，不修改系统 PATH。Skill 使用 DSh 的实例 `skills`、实例专用 `.agents\\skills` 和项目 `.dsh\\skills`/`.agents\\skills` 根；实例根可管理，项目根只读继承，因此同一 Source 项目的项目级 Skill 仍可能被多个版本共用。Agent Preset 使用实例 `.agent-presets`。Workflow 当前表示 DSh 随附 standard preset 提供的能力，不虚构独立目录。
- 插件市场：扩展页只显示插件市场，不再单独提供“已安装”页。市场从社区目录、GitHub `dsh-plugin` 标签和 Launcher 根目录下的可选自定义目录读取候选；明确的 DeepSeek 官方包/仓库才可标记为官方，当前实例中的依赖只作为 Installed 状态参与匹配。目录只负责发现，不直接代表可安装。市场结果写入 Launcher 根目录缓存，打开页面先显示缓存；搜索框输入使用内存中的完整 catalog 做 180ms 防抖即时筛选，只有“刷新目录”才联网。结果先按 UI、工具、工作流、Agent、模型、主题、开发分类浏览，来源是次级筛选；相同 npm/GitHub/monorepo 子路径身份的多来源条目合并保留描述、分类、Stars、更新时间和来源标签。目录中的 `dsh plugin --profile ... add <spec>` 安装命令会先提取真正的 Plugin spec，再进行 GitHub/npm 校验并交给官方 CLI，避免把整条 shell 命令作为包名传入。已安装 Plugin 显示已安装、可更新版本或更新状态未知，安装/更新/卸载继续调用官方 DSh Plugin CLI，且要求实例已停止、不是 Attached；操作前备份 web profile 配置，CLI 失败时尝试恢复已知 profile 文件，完成后自动重读左侧实例内容和右侧市场状态，并提示重启实例。市场变更操作显示阶段文字和 indeterminate 进度条，操作期间禁用其他 Plugin 变更及目录刷新。主题条目沿用 Plugin 目录格式；如果当前运行实例装有 `dsh-market`，Launcher 只通过其 loopback `/dsh-market/installed` 和 `/dsh-market/use-skin` 接口读取状态、显示文字信息预览并应用主题，不修改 DSh Web UI。没有桥接时仍可浏览/安装主题资源但不显示可应用状态；Wallpaper 尚未建立独立格式。
- 版本设置与整合包：每个版本的设置保存于自己的 `DSH_HOME\\.dsh-launcher\\version-settings.json`。个性化页只显示当前版本信息，版本名称由“版本控制”页面维护，避免在两个页面修改同一注册数据。配置页支持全版本同步、版本独立、工作区同步和全量兜底同步；另有独立的“所有版本自动同步模型”开关，模型同步不再跟随对话文件范围。全版本同步打开时下方对话选项禁用，并使对话策略按所有版本处理。开启模型同步时，`ModelProviderSyncService` 按停止版本中最新的 Provider 配置，把 `settings.yaml` 的模型段和 Provider 启用状态复制到其它符合策略的停止版本；不复制 API Key 明文，不写入运行中的版本，也不覆盖其它 DSh 设置。插件管理页复用官方 Plugin CLI 和现有 `ExtensionService`，只对当前实例的可管理 Plugin 提供启用/禁用/删除。`.dshpack` 是默认扩展名的 ZIP 包，包含 `manifest.json` 和经过清理的 `dsh-home/` 设计配置；导出不包含 API Key、Token、环境变量值或 sessions，Provider 和 Plugin dependency 中的 URL 用户信息/query Token 也会清理，导入始终创建新的版本，不覆盖已有 DSH_HOME。扩展名可在导出页修改，包内路径必须留在 `dsh-home/` 内。
- `.dshpack` 的 manifest 记录名称、说明、DSh 版本、创建时间、Plugin/Skill/Agent Preset/Provider 摘要和 workflow；导出不包含 API Key、Token、环境变量值、Node.js 路径或 sessions，Skill/Agent Preset 文本在导出和导入时再次清理敏感值。导入先显示摘要预览，确认后创建新的独立版本。
- 插件市场验证：候选插件安装前读取 npm 或 GitHub 的 `package.json`，检查 `dsh.bundle.patch` 和可加载入口；GitHub 校验优先读取 `default_branch`，并支持 `tree/<branch>/<subpath>` 与 `#path:/...` monorepo 位置；没有包名/仓库、缺少 DSh bundle 或缺少入口的项目会被拒绝。Launcher 不直接修改 `dsh.profile.bundles` 或 `cordis.patch.yml`，也不承诺热更新。
- MCP：配置符合 DSh MCP client 的 `stdio` 或 `streamable-http` 结构，元数据保存在实例 `.dsh-launcher\\mcp.json`，启用项生成实例 `launcher.patch.yml`，启动时通过 `--patch` 叠加；配置与 Plugin/Skill/Preset 修改都要求实例停止。
- 模型：编辑实例 `settings.yaml` 中的 `llm-deepseek` 和 `llm-pi-ai.providers`；保留无关顶层段落，只写环境变量引用、URL 和模型目录，不写 API Key 明文。Provider 自动同步只重写这两个模型配置范围，并保留其它顶层段落。
- Provider 启动页状态：Provider 卡片只读检测连接、`/models` 模型列表和思考档位；绿色/红色圆点是 Launcher 保存于实例 `DSH_HOME\.dsh-launcher\providers.json` 的启用状态，不改写 DSh 的 `settings.yaml`。有问题时显示问号入口，弹出诊断与解决方法；Provider 配置由当前版本的 DSh `settings.yaml` 提供。
- 对话：按当前 DSh JSONL 持久化格式扫描实例 `sessions`，识别 `session.jsonl` 和 `session.jsonl.zstd`；压缩文件按首个 Zstandard frame 读取 session header，要求 `version=0`、安全非负整数 `createdAt`/`delegationDepth` 及当前字段类型，头部有效的会话可从 Chat 入口预选，文件可导入、导出、备份或删除并保留原始格式，修改类操作要求实例停止。双击有效会话时，已运行实例直接打开目标 Chat；已停止实例先启动并通过健康检查，再打开目标会话。当前使用已验证的 Chat `localStorage` 预选 session ID，尚未承诺官方 session deep link。无法读取 header 的文件仍列出但禁止打开；导出目标会归一化重复的 `.jsonl`/`.jsonl.zstd` 后缀。版本会话策略由 `ConversationSyncService` 执行：Independent 不合并，Workspace 按同名工作区合并，All 或“和所有版本配置同步”作为兜底加入所有版本；同步只在进入对话页、启动前、停止/重启边界和会话导入/删除后处理停止实例，运行中的 DSh 不被直接写入。删除会话使用实例 `.dsh-launcher\conversation-sync.json` 的删除标记，避免旧文件在下一次同步时复活；重新创建同一路径的新文件会清除对应标记。
- Node/DSh 检测：检查 PATH 和 Windows 常见目录，实际执行版本命令；多个 Node 候选优先选择最高可解析版本，并从已检测 DSh 包或 Source package metadata 读取 `engines.node`，显示 `Missing`、`Compatible`、`Incompatible` 或 `Unknown`。官方包未声明 engine 时不额外写死版本限制。单候选检测超时会清理进程树，窗口关闭会取消检测；Windows `.cmd` 入口通过 `cmd.exe` 调用。Node.js 下载提供官方页和 npmmirror，DSh 安装提供 npm 官方源和 npmmirror 选择。
- 一键运行环境准备：设置/诊断页显示 Node 与 DSh 状态，环境缺失时提供“准备运行环境（官方源 / 国内镜像）”，全部由用户主动触发，启动时不静默下载或安装。Node 缺失时通过 NodeInstallService 下载 Windows x64 Node.js 官方 MSI（真实字节/百分比进度）并经系统授权（msiexec /qn）安装，完成后重新运行 NodeRuntimeDetector；Node 检测覆盖 `<ProgramFiles>\nodejs`（官方 MSI 默认位置），因此安装后无需重启 Launcher。Node 就绪但 DSh 缺失且目标是 Installed（或未指定目标的设置页）时复用 DshInstallService 通过 npm 安装 `@deepseek-ai/dsh`（不确定进度，不伪造百分比），安装后重新检测 DSh，多次重试仍检测不到时返回失败；Source 实例不安装全局 DSh，直接继续其项目自备 CLI 的启动流程。Node 版本不兼容时只提示安装兼容版本，不自动卸载。启动实例前发现运行环境缺失会列出缺失项并提供“准备运行环境”，准备成功后继续原启动流程。Node 下载阶段可取消并清理临时 `.part` 文件，此时关闭进度窗口等价于取消下载；MSI 系统安装开始后禁用取消按钮、阻止通过窗口 X 关闭进度窗口并阻止主窗口关闭（流程结束恢复后自动解除）、不强制终止 Windows Installer，用户取消与真实 10 分钟安装超时使用独立结果状态区分。DSh 重新安装并检测成功后，绑定已失效的 Installed 实例会重绑定到重新检测到的 package root / executable / version（保留实例 Id 与 DSH_HOME，不创建新实例，不修改 Source 实例的 root；运行中或 Attached 实例不参与重绑定）。Node 兼容判断以 metadata 为准：Installed 实例优先读取自身 package root 的 `engines.node`，有效但未声明时保持未声明，仅当其 runtime 失效且重装/重绑定时才使用重新检测到的 DSh metadata；Source 实例只读取自身项目 metadata，未声明时保持未声明，不继承全局 installed DSh 的版本要求；未选择实例的诊断场景使用全局 DSh engine。Node 版本选择同样以目标实例 engine 为准（Source 用自身 `engines.node` 选择兼容 LTS）；Node 检测进行中禁止启动实例。runtime 准备始终针对最初点击启动的实例：准备期间切换 SelectedInstance 不会改变启动目标，准备完成后按原实例 ID 重新解析并重新读取 engines.node 后再判定就绪。
