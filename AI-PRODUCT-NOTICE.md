# 本仓库是"完全由 AI 生成"的产物（声明）

> **请维护者与读者注意**：这个 fork 的**代码与文档完全由 AI 生成**；人类（仓库所有者）负责提出需求、做技术决策与验收实测。
> **是否正式合并到上游、以什么形式合并（整包 / 只取一块 / 完全不收），完全由上游作者决定。**

## 具体说明

- **生成方式**：AI 助手在本机工作区中编写与修改代码/文档；人负责需求、决策、验收
- **验证基线**（由人执行的验收，不是 AI 自称）：
  - `dotnet build -c Release src/DshLauncher/DshLauncher.csproj` → **0 警告 0 错误**
  - 仓库自测 `tests/DshLauncher.SelfTest` → **175 PASS / 0 FAIL**
  - 端到端 harness（含 UI，`_verify-p0`）→ **348 PASS / 0 FAIL**
  - 多轮真机验收（崩溃归因、终端启动、TUI 插件检测、自绘对话框等）
- **与上游的关系**：基于上游 **v1.0.7**；**未合并** v1.0.8 ~ v1.1.2；与 v1.1.2 有 **40 个重叠文件**，直接合并会产生冲突
- **如果上游不接受 AI 产物**：直接说明即可 —— 本仓库将作为个人 fork 保留，不再打扰

## 为什么单独写这份文件

本仓库的提交署名是 `local-dev <local-dev@localhost>`，**从提交历史里看不出 AI 参与**。
为了让"这是 AI 生成物"这件事**随代码一起传播**（而不是只出现在 PR 描述里），特此显式声明。
